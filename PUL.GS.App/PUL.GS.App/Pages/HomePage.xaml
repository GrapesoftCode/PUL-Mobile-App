<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="PUL.GS.App.Pages.HomePage"
             Visual="Material"
             NavigationPage.HasNavigationBar="True">
    <ContentPage.Content>
        <StackLayout
            Orientation="Horizontal">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height=".8*"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height=".8*"></RowDefinition>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" BackgroundColor="#ffffff" Margin="0,20,0,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"></RowDefinition>
                        <RowDefinition Height="auto"></RowDefinition>
                    </Grid.RowDefinitions>

                    <Label Text="Establecimientos" Grid.Row="0" Margin="5,0,0,0"></Label>
                    <CollectionView 
                        ItemsSource="{Binding Establishments}" 
                        Grid.Row="1" 
                        BackgroundColor="#ffffff"
                        SelectionMode="Single"
                        SelectedItem="{Binding CurrentEstablishment}"
                        SelectionChangedCommand="{Binding EstablishmentCommand}">
                        <CollectionView.ItemsLayout>
                            <GridItemsLayout Orientation="Horizontal"/>
                        </CollectionView.ItemsLayout>
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Frame Padding="5,0,5,10"
                                    IsClippedToBounds="True">
                                    <Image Source="{Binding Logo.Uri}" Aspect="AspectFit"></Image>
                                </Frame>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </Grid>

                <Grid Grid.Row="1" BackgroundColor="#ffffff" Margin="0,20,0,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"></RowDefinition>
                        <RowDefinition Height="auto"></RowDefinition>
                    </Grid.RowDefinitions>

                    <Label Text="Promociones" Grid.Row="0" Margin="5,0,0,0"></Label>
                    <CollectionView ItemsSource="{Binding Promotions}" Grid.Row="1" BackgroundColor="#ffffff">
                        <CollectionView.ItemsLayout>
                            <GridItemsLayout Orientation="Horizontal"/>
                        </CollectionView.ItemsLayout>
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Frame Padding="5,0,5,10"
                                       IsClippedToBounds="True">
                                    <Grid>
                                        <Image 
                                        Source="{Binding Logo.Uri}"
                                        Aspect="AspectFit"></Image>
                                    </Grid>
                                </Frame>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </Grid>

                <Grid Grid.Row="2" BackgroundColor="#ffffff" Margin="0,20,0,20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"></RowDefinition>
                        <RowDefinition Height="auto"></RowDefinition>
                    </Grid.RowDefinitions>

                    <Label Text="Paquetes/Combos" Grid.Row="0" Margin="5,0,0,0"></Label>
                    <CollectionView 
                        ItemsSource="{Binding Combos}" 
                        Grid.Row="1" 
                        BackgroundColor="#ffffff"
                        SelectionMode="Single"
                        SelectedItem="{Binding CurrentCombo}"
                        SelectionChangedCommand="{Binding ComboCommand}">
                        <CollectionView.ItemsLayout>
                            <GridItemsLayout Orientation="Horizontal"/>
                        </CollectionView.ItemsLayout>
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Frame Padding="5,0,5,10"
                                    IsClippedToBounds="True">
                                    <Image Source="{Binding Logo.Uri}" Aspect="AspectFit"></Image>
                                </Frame>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </Grid>
            </Grid>

        </StackLayout>
    </ContentPage.Content>
    

</ContentPage>